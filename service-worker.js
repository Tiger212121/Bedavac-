// Bu boş service worker dosyası, tarayıcının uygulamayı
// kurulabilir (installable) olarak algılaması için yeterlidir.
// Şimdilik çevrimdışı özellikler eklemeyeceğiz.

self.addEventListener('fetch', (event) => {
  // Bu olay dinleyici, PWA'nın çalışması için gereklidir.
});
